// @flow strict

/*::
export type Frame<T> = {
  time: number,
  value: T
};

export type Track<T> = {
  initial: T,
  frames: Frame<T>[],
};

export type TrackInterpolation =
  | 'step'
  | 'linear'
*/
