// @flow strict

/*::
import type { DocNode } from "./assertions";

export type DocBlock<Parameters: { [parameterKey: string]: mixed }> = {
  executeBlock: (parameters: Parameters) => DocNode,
  parameterDefaults: Parameters,
  parameterTypes: { [parameterKey: string]: string },
};
*/
